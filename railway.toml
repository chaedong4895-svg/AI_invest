[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/reports?limit=1"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
